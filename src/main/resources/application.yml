spring:
  application:
    name: blog

app:
  jwt:
    secret: changeme-this-is-a-very-long-secret-key-for-hs256-at-least-256-bits
    expiration: 86400000 # 24h en ms

springdoc:
  api-docs:
    path: /api-docs
  swagger-ui:
    path: /swagger-ui.html
